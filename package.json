{
  "name": "@lociere/glimmer-cradle-napcat-adapter",
  "version": "0.2.2",
  "private": false,
  "description": "Third-party NapCat integration for Glimmer Cradle.",
  "license": "GPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lociere/glimmer-cradle-napcat-adapter.git"
  },
  "packageManager": "pnpm@11.13.0",
  "engines": {
    "node": ">=24.18.0 <25",
    "pnpm": "11.13.0"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "link:local-sdk": "node scripts/link-local-sdk.mjs",
    "build": "node scripts/clean-dist.mjs && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "validate:manifest": "node scripts/validate-manifest.mjs",
    "validate": "pnpm validate:manifest && pnpm typecheck && pnpm test",
    "release:prepare": "pnpm validate && pnpm build && node scripts/build-release.mjs"
  },
  "peerDependencies": {
    "@glimmer-cradle/extension-sdk": "0.2.2",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^6.0.2",
    "vitest": "^1.6.1",
    "yaml": "^2.8.1",
    "zod": "^3.23.0"
  }
}

